<!DOCTYPE html>
<html lang="en">

<head>
  <%- include ../layout/head %>
  <title>Book App</title>
</head>

<body>

  <header>
    <%- include ../layout/header %>
  </header>

  <main>

    <h2> <%= totalBooks %> Saved Books</h2>\
      <% collection.forEach(book =>{ %>
      <div class="book">
        <%- include('./books/detail', {book: book}) %>
        <a href='/book/<%= book.id%>'>View Details</a>
      </div>
      <% }) %>

  </main>
</body>

</html>